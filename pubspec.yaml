name: bankio
description: "Payments. Reformulated. Improved. Unbeatable."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  app_links: ^6.4.1
  firebase_messaging: ^16.0.1
  flutter:
    sdk: flutter
  google_fonts: ^6.3.0
  http: ^1.5.0
  mobile_scanner: ^7.0.1
  path_provider: ^2.1.5
  qr_flutter: ^4.1.0
  url_launcher: ^6.3.2
  web_socket_channel: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
